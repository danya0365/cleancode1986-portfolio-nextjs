"use client";

import { TermsPremiumView } from "./TermsPremiumView";

export function TermsTerminalView() {
  // Currently sharing the premium view until a proper terminal view is fully designed
  return <TermsPremiumView />;
}
